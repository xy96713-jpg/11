# 🛠️ 系统工具 (System Tools / Environment Enabler)

本模块为您提供 Chrome 及其周边环境的底层增强与维护能力。它旨在将您的 Chrome 打造为一个无视区锁、原生集成长上下文 AI 能力的专家级环境。

## 🌟 核心能力

- **Chrome AI 全量解锁**：激活 `Glic` 侧边栏、`Prompt API`、`Summarize API` 及 `Gemini Nano` 本地模型。
- **地理伪装 (Geo-Bypass)**：强制锁定 `Variations Country` 为 `US`，绕过 NotebookLM 及 AI 服务的区域封锁。
- **组件监控**：实时审计 Chrome Components 是否已下载 `Gemini Nano` 权重。
- **进程管理**：自动化静默关闭/重启 Chrome 进程，以应用最新的底层补丁。

---

## 📖 用户操作指南 (User Manual)

### 1. 怎么一键解锁全量 AI？
直接要求我执行激活逻辑：
- **示例**：`“帮我解锁 Chrome 的所有隐藏 AI 功能”`
- **示例**：`“我的 window.ai 还是 undefined，帮我修复一下”`

### 2. 怎么切换到美国环境？
- **示例**：`“把我的浏览器环境改为美国区域，我要用 NotebookLM”`

### 3. 如何健康检查？
- **示例**：`“帮我诊断一下 Chrome 的 AI 运行环境”`

---

## 🤖 AI 激活指令 (Agent Activation)

若需 AI 助手进入系统级维护模式，请尝试以下关键词：

- **“使用 ChromeAIEnabler 模块修复...”**
- **“执行全量 AI 补丁过程”**
- **“调用系统工具模块审计...”**
- **“切换 ChromeVariations 为美国区域”**

> **AI 行为逻辑**：Agent 会启动 `assets/main.py`（基于 lcandy2 专家逻辑），自动遍历 Stable/Canary 路径并注入 `is_glic_eligible` 开关。

---

## 🛠️ 技术存档 (Tech Specs)
- **版本**: V2.5 (Enabler Focus)
- **更新时间**: 2026-01-31
- **资产池**: `assets/` 内存放了补丁主脚本以及 10+ 个用于可视化诊断的 Python 脚本。
