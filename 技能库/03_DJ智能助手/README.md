# 🎧 DJ 智能助手 (DJ Intelligence / Master DJ)

这是专为顶级 DJ 与制作人设计的模块。它能深度联动 Rekordbox，实现从数据库审计到全自动标点（Cue Points）的专业演奏流支持。

## 🌟 核心能力

- **Rekordbox 深度同步**：秒级解析 `xml` 数据库，同步所有曲库变更与收藏夹。
- **AI 标点 (Smart HotCues)**：基于 4/8/16/32 小节的乐句逻辑，在 Drop、Break 及乐句转换点自动设下标点。
- **混音和谐度审计**：计算 Key、Energy 及 BPM 的冲突概率，提供 Mashup 建议。
- **零交叉点检测**：确保所有 Cue 点均避开震荡中心点，杜绝现场数字噪音。

---

## 📖 用户操作指南 (User Manual)

### 1. 怎么生成 Cue 点？
您可以发送您的歌曲文件（wav/mp3），并直接要求：
- **示例**：`“帮我给 Super Shy 标点，我要 32 小节乐句逻辑的”`

### 2. 怎么检查数据库？
只要您的 `collection.xml` 在指定目录，您可以：
- **示例**：`“检查我的曲库有没有损坏的链接”` 或 `“整理一下我的 Rekordbox 收藏夹”`

### 3. 询问混音建议
- **示例**：`“这两首歌（A 和 B）的相位和谐吗？适合在哪切入？”`

---

## 🤖 AI 激活指令 (Agent Activation)

若需 AI 助手进入 DJ 专家模式，请尝试以下关键词：

- **“以 DJ Master 身份审计...”**
- **“调用 RekordboxXML 模块更新...”**
- **“触发智能标点生成，针对...”**
- **“评估 A 与 B 的 Harmonic 和谐度”**

> **AI 行为逻辑**：Agent 会启动 `assets/` 中的 `audit_xml.py` 或 `skill_pro_cueing.py`，并前置检查是否存在“人声冲突”或“低音抵消”。

---

## 🛠️ 技术存档 (Tech Specs)
- **版本**: V4.0 Ultra+ (Performance Grade)
- **更新时间**: 2026-01-31
- **资产池**: `assets/` 内存放了 `cue_generator.py`, `rekordbox.py` 等 50+ 个脚本，覆盖了所有 DJ 核心算法。
